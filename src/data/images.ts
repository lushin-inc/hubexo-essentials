// ┌──────────────────────────────────────────────────────────────────┐
// │  IMAGES                                                            │
// │                                                                    │
// │  Everything here is OPTIONAL. Leave a value undefined to use the   │
// │  built-in fallback visual:                                         │
// │    • hero          → the Success Triangle diagram                  │
// │    • session thumb → a navy tile with the session number           │
// │    • session banner→ a navy panel with number + eyebrow + title    │
// │                                                                    │
// │  To drop in a real photo later, put the file in public/img/ and    │
// │  set the path here (relative to /public), e.g. 'img/hero.jpg'.     │
// │  Session thumbnail/banner images live on each session in           │
// │  sessions.ts (thumbnailSrc / bannerSrc) for the same reason.       │
// └──────────────────────────────────────────────────────────────────┘
export const heroImageSrc: string | undefined = undefined
